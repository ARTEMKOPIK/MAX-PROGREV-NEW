name: 🚀 Atlantis Grev - CI/CD Pipeline

on:
  push:
    branches: [ main, atlantis-grev-final-with-ci ]
  pull_request:
    branches: [ main ]

jobs:
  # ========================================
  # Job 1: Backend API тестирование
  # ========================================
  backend-api:
    name: 🌐 Backend API
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout код
      uses: actions/checkout@v4
    
    - name: 🔧 Установка .NET 8.0
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: 📦 Восстановление зависимостей
      run: |
        cd atlantis-grev/backend/AtlantisGrev.API
        dotnet restore
    
    - name: 🏗️ Сборка проекта
      run: |
        cd atlantis-grev/backend/AtlantisGrev.API
        dotnet build --configuration Release --no-restore
    
    - name: ✅ Проверка успешной сборки
      run: echo "✅ Backend API успешно собран!"

  # ========================================
  # Job 2: Warming Service тестирование
  # ========================================
  warming-service:
    name: 🔄 Warming Service
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout код
      uses: actions/checkout@v4
    
    - name: 🔧 Установка .NET 8.0
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: 📦 Восстановление зависимостей
      run: |
        cd atlantis-grev/warming-service/AtlantisGrev.WarmingService
        dotnet restore
    
    - name: 🏗️ Сборка проекта
      run: |
        cd atlantis-grev/warming-service/AtlantisGrev.WarmingService
        dotnet build --configuration Release --no-restore
    
    - name: ✅ Проверка успешной сборки
      run: echo "✅ Warming Service успешно собран!"

  # ========================================
  # Job 3: Mobile App тестирование
  # ========================================
  mobile-app:
    name: 📱 Mobile App
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout код
      uses: actions/checkout@v4
    
    - name: 🔧 Установка Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.24.0'
        channel: 'stable'
    
    - name: 📦 Проверка Flutter
      run: flutter doctor -v
    
    - name: 📦 Установка зависимостей
      run: |
        cd atlantis-grev/mobile/atlantis_grev_mobile
        flutter pub get
    
    - name: 🔍 Анализ кода
      run: |
        cd atlantis-grev/mobile/atlantis_grev_mobile
        flutter analyze
      continue-on-error: true
    
    - name: 🏗️ Сборка APK (debug)
      run: |
        cd atlantis-grev/mobile/atlantis_grev_mobile
        flutter build apk --debug
    
    - name: ✅ Проверка успешной сборки
      run: echo "✅ Mobile App успешно собран!"
    
    - name: 📦 Загрузка APK артефакта
      uses: actions/upload-artifact@v4
      with:
        name: atlantis-grev-debug-apk
        path: atlantis-grev/mobile/atlantis_grev_mobile/build/app/outputs/flutter-apk/app-debug.apk

  # ========================================
  # Job 4: Проверка документации
  # ========================================
  documentation:
    name: 📚 Документация
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout код
      uses: actions/checkout@v4
    
    - name: 🔍 Проверка наличия документации
      run: |
        echo "🔍 Проверка файлов документации..."
        
        if [ -f "atlantis-grev/README.md" ]; then
          echo "✅ README.md найден"
        else
          echo "❌ README.md не найден"
          exit 1
        fi
        
        if [ -f "atlantis-grev/QUICK_START.md" ]; then
          echo "✅ QUICK_START.md найден"
        else
          echo "❌ QUICK_START.md не найден"
          exit 1
        fi
        
        if [ -f "atlantis-grev/DEPLOYMENT.md" ]; then
          echo "✅ DEPLOYMENT.md найден"
        else
          echo "❌ DEPLOYMENT.md не найден"
          exit 1
        fi
        
        if [ -f "atlantis-grev/ARCHITECTURE.md" ]; then
          echo "✅ ARCHITECTURE.md найден"
        else
          echo "❌ ARCHITECTURE.md не найден"
          exit 1
        fi
        
        echo "✅ Вся документация на месте!"
    
    - name: 🔍 Проверка автоскриптов
      run: |
        echo "🔍 Проверка автоматических скриптов..."
        
        if [ -f "atlantis-grev/install.bat" ]; then
          echo "✅ install.bat найден"
        else
          echo "❌ install.bat не найден"
          exit 1
        fi
        
        if [ -f "atlantis-grev/install.sh" ]; then
          echo "✅ install.sh найден"
        else
          echo "❌ install.sh не найден"
          exit 1
        fi
        
        if [ -f "atlantis-grev/start.bat" ]; then
          echo "✅ start.bat найден"
        else
          echo "❌ start.bat не найден"
          exit 1
        fi
        
        if [ -f "atlantis-grev/start.sh" ]; then
          echo "✅ start.sh найден"
        else
          echo "❌ start.sh не найден"
          exit 1
        fi
        
        if [ -f "atlantis-grev/config.txt" ]; then
          echo "✅ config.txt найден"
        else
          echo "❌ config.txt не найден"
          exit 1
        fi
        
        echo "✅ Все скрипты на месте!"

  # ========================================
  # Job 5: Финальная проверка
  # ========================================
  final-check:
    name: 🎯 Финальная проверка
    runs-on: ubuntu-latest
    needs: [backend-api, warming-service, mobile-app, documentation]
    
    steps:
    - name: 🎉 Все тесты пройдены!
      run: |
        echo "=================================="
        echo "🎉 ВСЕ ТЕСТЫ УСПЕШНО ПРОЙДЕНЫ!"
        echo "=================================="
        echo ""
        echo "✅ Backend API - скомпилирован"
        echo "✅ Warming Service - скомпилирован"
        echo "✅ Mobile App - собран"
        echo "✅ Документация - проверена"
        echo "✅ Автоскрипты - на месте"
        echo ""
        echo "🚀 Проект готов к использованию!"
        echo "=================================="

