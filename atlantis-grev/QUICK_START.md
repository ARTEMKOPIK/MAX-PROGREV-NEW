# 🚀 БЫСТРЫЙ СТАРТ - Atlantis Grev

## ⚡ 5 ПРОСТЫХ ШАГОВ

### Шаг 1: Скачай проект
```bash
git clone https://github.com/ARTEMKOPIK/MAX-PROGREV-NEW.git
cd MAX-PROGREV-NEW/atlantis-grev
```

---

### Шаг 2: Получи ключи (5 минут)

#### 🗄️ Supabase (База данных)
1. Зайди на https://supabase.com
2. Создай новый проект
3. Зайди в "Settings" → "API"
4. Скопируй:
   - **Project URL** (например: `https://abcdefgh.supabase.co`)
   - **anon public** ключ (длинная строка)

5. Создай таблицы:
   - Открой "SQL Editor"
   - Скопируй SQL из файла `DEPLOYMENT.md` (раздел "Database Setup")
   - Нажми "Run"

#### 💰 Crypto Pay (Платежи)
1. Открой Telegram
2. Найди бота @CryptoBot
3. Создай приложение: `/api`
4. Скопируй **API Token**

---

### Шаг 3: Заполни config.txt

Открой файл `config.txt` и вставь свои данные:

```
SUPABASE_URL=https://твой-проект.supabase.co
SUPABASE_KEY=твой-supabase-ключ
CRYPTOPAY_TOKEN=твой-cryptopay-токен
```

**Остальное можно не трогать!**

---

### Шаг 4: Установи всё

#### Windows:
```bash
install.bat
```

#### Mac/Linux:
```bash
chmod +x install.sh
./install.sh
```

**Скрипт автоматически:**
- Проверит наличие .NET и Flutter
- Установит все зависимости
- Подготовит проект к запуску

---

### Шаг 5: Запусти всё!

#### Windows:
```bash
start.bat
```

#### Mac/Linux:
```bash
chmod +x start.sh
./start.sh
```

**Скрипт автоматически:**
- Настроит Backend с твоими ключами
- Запустит Backend API на порту 5000
- Запустит Warming Service на порту 5001

**Откроется 2 окна терминала - НЕ ЗАКРЫВАЙ ИХ!**

---

## ✅ ГОТОВО!

Теперь работает:
- 🌐 Backend API: http://localhost:5000
- 📚 Swagger UI: http://localhost:5000/swagger
- 🔄 Warming Service: http://localhost:5001

---

## 📱 ЗАПУСК МОБИЛЬНОГО ПРИЛОЖЕНИЯ

### Вариант А: На реальном телефоне (Android)

1. Включи "Режим разработчика" на телефоне:
   - Настройки → О телефоне → Номер сборки (тапай 7 раз)
   
2. Включи "Отладку по USB":
   - Настройки → Для разработчиков → Отладка по USB

3. Подключи телефон к компьютеру через USB

4. Запусти приложение:
```bash
cd mobile/atlantis_grev_mobile
flutter run
```

5. Подожди 2-3 минуты - приложение установится!

### Вариант Б: На эмуляторе Android

1. Установи Android Studio
2. Открой AVD Manager
3. Создай виртуальное устройство
4. Запусти эмулятор
5. Выполни команды:
```bash
cd mobile/atlantis_grev_mobile
flutter run
```

### Вариант В: Собрать APK для установки

```bash
cd mobile/atlantis_grev_mobile
flutter build apk --release
```

**APK будет здесь:** `build/app/outputs/flutter-apk/app-release.apk`

Скопируй его на телефон и установи!

---

## 🛑 ОСТАНОВКА

### Windows:
Закрой все окна терминала

### Mac/Linux:
```bash
./stop.sh
```

---

## ❓ ПРОБЛЕМЫ?

### Backend не запускается
- Проверь что .NET 8.0 установлен: `dotnet --version`
- Проверь что порт 5000 свободен
- Проверь что config.txt заполнен правильно

### Приложение не подключается
- Проверь что Backend запущен (открой http://localhost:5000/swagger)
- На реальном телефоне:
  - Узнай IP компьютера: `ipconfig` (Windows) или `ifconfig` (Mac/Linux)
  - В файле `mobile/atlantis_grev_mobile/lib/services/api_service.dart` замени `localhost` на IP компьютера
- На Android эмуляторе: используй `http://10.0.2.2:5000`

### Flutter не работает
```bash
flutter doctor
```
Исправь все ошибки которые покажет

---

## 🎉 ПОЗДРАВЛЯЮ!

Теперь у тебя работает полная система прогрева WhatsApp аккаунтов!

**Что можешь делать:**
- Регистрировать пользователей через Telegram ID
- Покупать аккаунты через Crypto Pay
- Автоматически прогревать аккаунты
- Управлять рефералами
- Выводить средства

---

## 💡 СОВЕТЫ

1. **Не закрывай терминалы** с Backend и Warming Service - они должны работать постоянно
2. **Для продакшена** - разверни на VPS сервере (см. DEPLOYMENT.md)
3. **Логи** находятся в консоли или в папке `logs/`
4. **Swagger UI** - удобный интерфейс для тестирования API

---

Нужна помощь? Пиши! 🚀

