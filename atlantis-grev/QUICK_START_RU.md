# 🚀 Быстрый Запуск Atlantis Grev на ПК

Пошаговая инструкция для запуска проекта на вашем компьютере.

---

## ⚡ Предварительные Требования

Убедитесь что установлено:
- ✅ .NET 8.0 SDK
- ✅ Flutter SDK
- ✅ Git
- ✅ Chrome/Chromium
- ✅ Создан проект в Supabase
- ✅ Получен токен Crypto Pay

---

## 📥 Шаг 1: Скачать Проект

### Вариант А: Если еще не скачали

```bash
# Откройте терминал (cmd/PowerShell на Windows, Terminal на Mac/Linux)

# Перейдите в папку где хотите сохранить проект
cd C:\Projects  # Windows
# или
cd ~/Projects   # Mac/Linux

# Клонируйте репозиторий
git clone https://github.com/ARTEMKOPIK/MAX-PROGREV-NEW.git

# Перейдите в папку проекта
cd MAX-PROGREV-NEW
```

### Вариант Б: Если уже скачали

```bash
# Просто перейдите в папку проекта
cd путь/к/MAX-PROGREV-NEW
```

---

## 🔧 Шаг 2: Настроить Backend API

### 2.1 Перейдите в папку Backend

```bash
cd atlantis-grev/backend/AtlantisGrev.API
```

### 2.2 Откройте appsettings.json

**Windows:**
```bash
notepad appsettings.json
```

**Mac/Linux:**
```bash
nano appsettings.json
# или
code appsettings.json  # если установлен VS Code
```

### 2.3 Вставьте Ваши Данные

Замените значения на свои:

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Supabase": {
    "Url": "https://ваш-проект.supabase.co",           // ← Замените!
    "AnonKey": "ваш-supabase-anon-ключ"                // ← Замените!
  },
  "CryptoPay": {
    "Token": "ваш-cryptopay-токен"                     // ← Замените!
  },
  "Jwt": {
    "Secret": "ВашСуперСекретныйКлючМинимум32Символа!",  // ← Замените!
    "Issuer": "AtlantisGrev",
    "Audience": "AtlantisGrevUsers",
    "ExpirationDays": "7"
  },
  "App": {
    "BaseUrl": "http://localhost:8080"
  },
  "WarmingService": {
    "Url": "http://localhost:5001"
  }
}
```

**Где взять данные:**
- **Supabase URL и AnonKey**: Supabase → Settings → API
- **CryptoPay Token**: Telegram → @CryptoBot → /api
- **JWT Secret**: Придумайте случайную строку (минимум 32 символа)

### 2.4 Сохраните Файл

**Windows (Notepad):** File → Save
**Mac/Linux (nano):** Ctrl+O → Enter → Ctrl+X

---

## 🔥 Шаг 3: Настроить Mobile App

### 3.1 Узнайте IP Адрес Вашего ПК

**Windows:**
```powershell
ipconfig
```
Найдите "IPv4 Address" (например: `192.168.1.100`)

**Mac:**
```bash
ifconfig en0 | grep inet
```

**Linux:**
```bash
ip addr show | grep inet
```

Запомните этот IP!

### 3.2 Откройте api_service.dart

```bash
# Из корня проекта:
cd atlantis-grev/mobile/atlantis_grev_mobile/lib/services
```

**Windows:**
```bash
notepad api_service.dart
```

**Mac/Linux:**
```bash
nano api_service.dart
# или
code api_service.dart
```

### 3.3 Измените baseUrl

Найдите строку:
```dart
static const String baseUrl = 'http://localhost:8080';
```

Замените на один из вариантов:

**Для тестирования на эмуляторе Android:**
```dart
static const String baseUrl = 'http://10.0.2.2:8080';
```

**Для тестирования на реальном устройстве:**
```dart
static const String baseUrl = 'http://192.168.1.100:8080';  // ← Ваш IP из шага 3.1
```

**Для iOS симулятора:**
```dart
static const String baseUrl = 'http://localhost:8080';  // localhost работает на iOS
```

### 3.4 Сохраните Файл

---

## 🎬 Шаг 4: ЗАПУСК ПРОЕКТА!

Теперь запускаем все компоненты! Откройте **3 терминала/командные строки**.

### Терминал 1: Backend API 🔌

```bash
# Перейдите в папку Backend API
cd atlantis-grev/backend/AtlantisGrev.API

# Установите зависимости (первый раз)
dotnet restore

# ЗАПУСТИТЕ API
dotnet run
```

**Вы увидите:**
```
info: Microsoft.Hosting.Lifetime[14]
      Now listening on: http://localhost:8080
info: Microsoft.Hosting.Lifetime[0]
      Application started. Press Ctrl+C to shut down.
```

✅ **API работает!**

**Проверка:** Откройте браузер → http://localhost:8080/swagger

### Терминал 2: Warming Service ⚙️

**НЕ ЗАКРЫВАЙТЕ первый терминал!** Откройте **второй**.

```bash
# Перейдите в папку Warming Service
cd atlantis-grev/warming-service/AtlantisGrev.WarmingService

# Установите зависимости (первый раз)
dotnet restore

# ЗАПУСТИТЕ СЕРВИС
dotnet run
```

**Вы увидите:**
```
info: AtlantisGrev.WarmingService.WarmingWorker[0]
      Warming worker started
info: AtlantisGrev.WarmingService.WarmingWorker[0]
      Listening for warming requests...
```

✅ **Warming Service работает!**

### Терминал 3: Mobile App 📱

**НЕ ЗАКРЫВАЙТЕ первые два терминала!** Откройте **третий**.

```bash
# Перейдите в папку Mobile App
cd atlantis-grev/mobile/atlantis_grev_mobile

# Установите зависимости (первый раз)
flutter pub get

# Проверьте подключенные устройства
flutter devices

# ЗАПУСТИТЕ ПРИЛОЖЕНИЕ
flutter run
```

**Если несколько устройств:**
```bash
# Выберите устройство:
flutter run -d chrome        # Для браузера (быстрее всего!)
flutter run -d windows       # Для Windows приложения
flutter run -d android       # Для Android эмулятора
```

**Вы увидите:**
```
Launching lib\main.dart on Chrome in debug mode...
✓ Built build\web\main.dart.js
```

✅ **Mobile App запущено!**

---

## 🧪 Шаг 5: Тестирование

### 5.1 Проверьте Backend API

1. Откройте браузер: http://localhost:8080/swagger
2. Вы должны увидеть список всех API endpoints
3. Попробуйте развернуть `/api/auth/login` → **Try it out**

### 5.2 Проверьте Mobile App

1. В приложении вы должны увидеть **Login Screen**
2. Введите тестовые данные:
   - **Telegram ID**: `123456789` (любое число)
   - **Username**: `testuser`
   - **Referral Code**: (пусто)
3. Нажмите **Login** 🔐

**Если успешно:**
- ✅ Вы перейдете на DashboardScreen
- ✅ В терминале Backend API увидите лог:
  ```
  info: AuthController[0]
        Login attempt for Telegram ID: 123456789
  ```
- ✅ В Supabase (Table Editor → users) появится новая запись!

---

## 📊 Проверка Supabase

1. Зайдите в Supabase → **Table Editor**
2. Откройте таблицу **users**
3. Вы должны увидеть вашего тестового пользователя:
   - `telegram_id`: 123456789
   - `username`: testuser
   - `affiliate_code`: автоматически сгенерированный код

✅ **ВСЕ РАБОТАЕТ!**

---

## 🎯 Краткая Шпаргалка

### Запуск всего проекта (3 команды):

```bash
# Терминал 1
cd atlantis-grev/backend/AtlantisGrev.API && dotnet run

# Терминал 2
cd atlantis-grev/warming-service/AtlantisGrev.WarmingService && dotnet run

# Терминал 3
cd atlantis-grev/mobile/atlantis_grev_mobile && flutter run
```

### Остановка проекта:

В каждом терминале нажмите: **Ctrl+C**

---

## 🛠️ Полезные Команды

### Очистка и пересборка Backend:

```bash
cd atlantis-grev/backend/AtlantisGrev.API
dotnet clean
dotnet build
dotnet run
```

### Очистка и пересборка Mobile App:

```bash
cd atlantis-grev/mobile/atlantis_grev_mobile
flutter clean
flutter pub get
flutter run
```

### Просмотр логов Backend:

```bash
# Логи будут в терминале где запущен `dotnet run`
```

---

## 🐛 Решение Проблем

### Проблема: "Port 8080 already in use"

**Решение:**

**Windows:**
```powershell
# Найдите процесс на порту 8080
netstat -ano | findstr :8080

# Убейте процесс (замените PID на найденный)
taskkill /PID 1234 /F
```

**Mac/Linux:**
```bash
# Найдите и убейте процесс
lsof -ti:8080 | xargs kill -9
```

Или измените порт в `appsettings.json`:
```json
"Kestrel": {
  "Endpoints": {
    "Http": {
      "Url": "http://localhost:8081"  // Другой порт
    }
  }
}
```

### Проблема: Mobile App не подключается к API

**Проверьте:**

1. **Backend API запущен?** (проверьте терминал 1)
2. **Правильный baseUrl?** (проверьте api_service.dart)
3. **Устройство в той же сети?** (для реальных устройств)
4. **Firewall блокирует?** (временно отключите для теста)

**Windows - Разрешите порт в Firewall:**
```powershell
netsh advfirewall firewall add rule name="Atlantis Grev API" dir=in action=allow protocol=TCP localport=8080
```

### Проблема: Supabase ошибка подключения

**Проверьте:**

1. **Правильный URL и AnonKey** в appsettings.json
2. **Таблицы созданы** в Supabase (Table Editor)
3. **Проект активен** в Supabase

**Тест подключения:**

В Swagger UI попробуйте endpoint `/api/auth/login`:
```json
{
  "telegramId": 999999999,
  "username": "test",
  "referralCode": ""
}
```

Если ошибка - проверьте логи в терминале Backend API.

### Проблема: Flutter не находит устройства

```bash
# Проверьте доступные устройства
flutter devices

# Если пусто - установите эмулятор или запустите в браузере
flutter run -d chrome
```

### Проблема: .NET SDK не найден

```bash
# Проверьте версию
dotnet --version

# Если не найдено - установите .NET 8.0 SDK:
# https://dotnet.microsoft.com/download/dotnet/8.0
```

### Проблема: Flutter не установлен

```bash
# Проверьте Flutter
flutter doctor

# Установите недостающие компоненты по подсказкам
```

---

## 📱 Запуск на Реальном Устройстве

### Android:

1. **Включите режим разработчика** на телефоне
2. **Включите USB отладку**
3. **Подключите телефон к ПК**
4. **Разрешите USB отладку** на телефоне
5. Проверьте: `flutter devices`
6. Запустите: `flutter run`

### iOS (только на Mac):

1. **Подключите iPhone к Mac**
2. **Доверьтесь компьютеру** на iPhone
3. Проверьте: `flutter devices`
4. Запустите: `flutter run`

---

## 🎉 Готово!

Теперь у вас запущен полный проект:

- ✅ **Backend API** на http://localhost:8080
- ✅ **Warming Service** на http://localhost:5001
- ✅ **Mobile App** на выбранном устройстве
- ✅ **Supabase** база данных подключена
- ✅ **Crypto Pay** готов к приему платежей

---

## 📞 Следующие Шаги

1. **Протестируйте функционал**:
   - Регистрация пользователя ✅
   - Покупка аккаунтов (нужен реальный Crypto Pay)
   - Просмотр рефералов
   - Управление прогревом

2. **Настройте Webhook Crypto Pay**:
   - В @CryptoBot настройте webhook URL
   - Для локального тестирования используйте ngrok

3. **Деплой на сервер** (когда будете готовы):
   - Смотрите `DEPLOYMENT.md`

---

**Приятной работы с Atlantis Grev! 🚀**

